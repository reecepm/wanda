// happy-dom is already selected via vitest.config.ts `environment`. This
// file is a spot for any DOM-level polyfills or test hooks; the stub
// exports ensure vitest loads it when we add one.

export {}
